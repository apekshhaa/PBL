# docker-compose.yml content removed per request
# The Docker compose configuration was causing problems and has been
# neutralized. If you want the file deleted instead, tell me and I
# will remove it from the repository.
